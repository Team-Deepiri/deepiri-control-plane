# Control plane k8s

Same layout as **deepiri-platform** but `generate-env-files.sh` syncs the **full** service list (Cyrex, LIS, speech, messaging, etc.).

Secrets: existing Discord **`secrets.7z`** → this repo (edited). Do not use cloud **`cloud-portal-secrets.7z`** here.

See [`secrets-templates/SECRETS_SPLIT.md`](secrets-templates/SECRETS_SPLIT.md) (shared doc from platform repo).
